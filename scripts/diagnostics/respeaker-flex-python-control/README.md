# ReSpeaker Flex python_control

Vendored from [reSpeaker_Flex `python_control`](https://github.com/respeaker/reSpeaker_Flex/tree/main/python_control). Requires `pyusb` (installed via `pyproject.toml` / `setup.sh`).

```bash
cd ~/robot-pet
source .venv/bin/activate
python scripts/diagnostics/respeaker-flex-python-control/xvf_host.py --help
```
