[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "robo-pet"
version = "0.1.0"
description = "Raspberry Pi robot controller services and tools"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
    "basicmicro",
    "evdev; sys_platform == 'linux'",
    "gpiozero",
    "numpy",
    "openai",
    "pyserial",
    "pyusb",
    "rich",
    "smbus2; sys_platform == 'linux'",
    "certifi",
    "sounddevice",
    "textual",
    "websockets",
]

# Pi-only range sensor stack (Blinka + mux + VL53). Installed on the robot via
# setup.sh; kept out of default deps so Mac dev venvs stay lean.
[project.optional-dependencies]
pi-range = [
    "adafruit-blinka; sys_platform == 'linux'",
    "adafruit-circuitpython-tca9548a; sys_platform == 'linux'",
    "adafruit-circuitpython-vl53l0x; sys_platform == 'linux'",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["config", "control", "drivers", "lib", "telemetry", "voice"]
py-modules = ["gamepad_teleop", "robot_brain", "robot_camera", "robot_dashboard", "robot_telemetry", "robot_vision", "robot_voice", "robot_web_dashboard"]
